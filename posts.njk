---
layout: home.njk
title: Posts
subtitle: Things I thought one time, presented in reverse chronological order. It's a blog.
---

<div class="post-cards">
  {%- for post in collections.posts | reverse -%}
    <div class="single-post">
      <ul class="tags">
        {%- for tag in post.data.tags -%}
          {%- if tag != 'posts' -%}
            <li><a href="/tags/{{ tag }}/">{{ tag }}</a></li>
          {%- endif %}
        {%- endfor %}
      </ul>
      <h2><a href="{{ post.data.url }}">{{ post.data.title }}</a></h2>
      <p>{{ post.data.subtitle }}</p>
    </div>
  {%- endfor %}
</div>

<footer>
  <p>Subscribe via <a href="/feed.xml">RSS</a>.</p>
</footer>
